"""Densa's read-only MCP surface.

:mod:`densa.mcp.tools` holds the eight transport-free tool functions and
their JSON-Schema declarations; :mod:`densa.mcp.server` is the stdio
JSON-RPC 2.0 loop that exposes them to an MCP host. ``SPEC.md`` (in this
directory) is the frozen pre-launch contract both must match.
"""

from __future__ import annotations
