{
  "name": "densa",
  "version": "0.5.0",
  "description": "Slash commands (/ingest, /query, /lint, /process-inbox, /promote) for a Densa vault. The canonical contract lives in the vault's AGENTS.md; this plugin only wires up the prompts.",
  "commands": ".claude/commands"
}
