Run the canonical `ingest` procedure from `_system/prompts/ingest.md`
(path relative to the Densa vault root) against `$ARGUMENTS`. Plan
first; wait for my approval before applying any edits. If the
current workspace is not a Densa vault clone (no `AGENTS.md` at
root), stop and ask me to open one.
