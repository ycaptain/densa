Run the canonical `query` procedure from `_system/prompts/query.md`
(path relative to the Densa vault root) to answer: $ARGUMENTS. Cite
wiki pages inline; for substantive answers, file the Q&A back to
`outputs/qa/<YYYY-MM-DD>-<slug>.md` (`type: report`). If the Q&A
later proves evergreen, the human can run `promote` to lift it into
the wiki — do not edit `wiki/syntheses/` from inside a `query`. If
the current workspace is not a Densa vault clone, stop and ask me to
open one.
